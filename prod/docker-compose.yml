version: '3'

services:
  payload:
    build:
      context: ..
      dockerfile: Dockerfile
    #ports:
    #  - '3000:3000'
    networks:
      - nginx
      - shared_postgres
    env_file:
      - .env

networks:
  nginx:
    external: true
  shared_postgres:
    external: true
